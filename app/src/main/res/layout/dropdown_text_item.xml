<?xml version="1.0" encoding="utf-8"?>
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/text1"
    android:layout_width="match_parent"
    android:layout_height="@dimen/dropdown_height"
    android:layout_marginLeft="@dimen/dropdown_margin_start"
    android:layout_marginStart="@dimen/dropdown_margin_start"
    android:background="@color/background_dropdown"
    android:gravity="start|center_vertical"
    android:lines="1"
    android:maxLines="1"
    android:minLines="1"
    android:textAllCaps="false"
    android:textColor="@color/text_color"
    android:textColorHint="@color/text_color"
    android:textSize="@dimen/dropdown_text_size"
    android:textStyle="normal" />